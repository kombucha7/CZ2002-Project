/**
 * Interface for classes that print outputs for staff to view Functions tend to
 * parse through entire ArrayList and print indexes that match the options
 */
public interface ListPrinter {

	/**
	 * prints out objects in an arrayList that matches the selected conditions
	 */
	void printList();

}